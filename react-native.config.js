/*assets/fonts = your house
react-native.config.js = the address note
npx react-native-asset = the delivery person

Without the note, the delivery person doesn't know where to go. */

module.exports={
    project:{
        ios:{},
        android:{},
    },
    assets:['./assets/fonts']
}